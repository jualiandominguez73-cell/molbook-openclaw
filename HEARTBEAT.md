# HEARTBEAT.md

## Note: Sessions should have a short machine-generated title/description (derived from the first user message) for dashboards and status UIs.

## Note: If a session is missing a derived title, prefer generating/persisting it at session creation time (task inception) so UIs don’t need to re-derive it from transcripts.
