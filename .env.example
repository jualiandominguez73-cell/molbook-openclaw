# Copy to .env and fill with your Twilio credentials
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
# Must be a WhatsApp-enabled Twilio number, prefixed with whatsapp:
TWILIO_WHATSAPP_FROM=whatsapp:+17343367101

# Google Gemini API Keys
# Single key (legacy):
# GEMINI_API_KEY=your_key_here
#
# Multiple keys for rotation (comma, semicolon, or space-separated):
# GEMINI_API_KEYS=key1,key2,key3
#
# Or use numbered environment variables:
# GEMINI_API_KEY_1=first_key
# GEMINI_API_KEY_2=second_key
# GEMINI_API_KEY_3=third_key
#
# Keys are rotated automatically on 429 (rate limit) errors.
# Cooldown uses exponential backoff: 1min → 5min → 25min → 60min max.
