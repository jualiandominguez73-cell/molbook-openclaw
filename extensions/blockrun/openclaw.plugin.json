{
  "id": "blockrun",
  "name": "BlockRun",
  "description": "30+ AI models with smart routing and x402 micropayments. No API key — pay per request with USDC.",
  "providers": ["blockrun"],
  "configSchema": {
    "type": "object",
    "properties": {
      "walletKey": {
        "type": "string",
        "description": "EVM wallet private key (0x...). Auto-generated if not set."
      },
      "routing": {
        "type": "object",
        "description": "Override default routing configuration"
      }
    }
  },
  "uiHints": {
    "walletKey": {
      "label": "Wallet Private Key",
      "sensitive": true,
      "placeholder": "0x... (optional — auto-generated)"
    }
  }
}
